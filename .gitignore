# Generated artifact (rebuilt by make_motd.py / install.sh)
motd

# Local backups
*.bak.*

# Per-device diagnostic snapshot (generated by `ter snapshot`)
device.lock

# Secrets — only the template is tracked; live values live in
# ~/.config/ter/secrets.env (outside this repo).
secrets.env
